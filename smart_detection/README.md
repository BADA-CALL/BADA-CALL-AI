# 📱 Smart Accident Detection System

스마트폰의 가속도와 자이로스코프 센서를 활용하여 바다 위에서 발생하는 비정상적인 움직임(추락, 전복)을 감지합니다.

## ⚙️ Workflow

1. **Model Training**: Kaggle의 낙상 데이터를 학습하여 `har_model.tflite` 경량화 모델 생성.
2. **Data Streaming**: 'Sensor Logger' 앱을 통해 스마트폰 데이터를 실시간으로 Flask 서버에 전송.
3. **Hybrid Logic**: AI의 판단 확률과 물리적인 충격량(G-force)을 결합하여 오탐지율을 최소화.

## 📝 How to Use

1. `train_har_model.py`를 실행하여 TFLite 모델을 준비합니다.
2. `app_server.py`를 실행하여 Ngrok 주소를 확인합니다.
3. 스마트폰 앱 설정에서 해당 주소의 `/predict` 엔드포인트로 JSON 데이터를 전송합니다.

## 🚨 Decision Logic

- **AI Confidence**: > 0.15
- **Max Acceleration**: > 11.5 m/s²
- 두 조건이 동시에 충족될 때 **사고 발생**으로 판정합니다.
